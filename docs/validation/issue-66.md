# 이슈 #66 검증 기록

2026-09-13, Windows 11 · Git Bash · Python 3.12.10. 도시는 광주, 기관은 광주광역시청 하나다.
기준은 [폴백 정책](../specs/header-mapping-fallback.md)이고, 이 이슈가 이어받은 실측은
[#62 검증 기록](issue-62.md)이다.

[#66](https://github.com/snowjaewon/OfficialDeliciousMap/issues/66)은 세 가지와 그 셋을 반영한
`data/gwangju/` 재생성을 묶은 이슈이고, **두 번의 실행**으로 나뉘어 진행됐다. 아래를 순서대로
적는다.

| 실행 | 한 것 | 기반 | 산출물 |
| --- | --- | --- | --- |
| 1차 (PR #90) | 미해결 원본의 후보 수 | `develop` `10022b1` | 계약만 바꾸고 임시 data-root에서 실측 |
| 2차 (이 문서 뒤쪽) | PDF 헤더 판정 1회 · 전 단계 재생성 | `develop` `08f1f82` | 커밋된 `data/gwangju/`를 바꿨다 |

`confirmed_by` 16줄은 두 실행 모두에서 채우지 않았다. 담당자가 원본을 열어 채운다.

## 1차 — 미해결 원본의 후보 수 (PR #90)

이 절은 `GEMINI_API_KEY`가 없던 때의 기록이다. 아래 표의 "막은 것"은 2차에서 풀렸다.

| 이슈가 요구한 것 | 이번 결과 | 막은 것 |
| --- | --- | --- |
| PDF 3개의 헤더 판정 1회 | **하지 않음** | `GEMINI_API_KEY`와 단가 두 값이 `.env`에 비어 있다 |
| 16개 원본의 `confirmed_by` | **하지 않음** | 폴백 정책이 요구하는 사람 확인이며 담당자의 몫이다 |
| 미해결 원본의 후보 수 | **했다** | — |
| `data/gwangju/` 재생성 | **하지 않음**(2026-09-13 사용자 결정) | 위 둘이 갖춰진 뒤에 한다 |

사용자 결정(2026-09-13): **키 없이 재생성하지 않는다.** 지금 재생성하면 PDF 3개는 그대로
미해결이고 +13 레코드가 들어오지 않아, 키가 생긴 뒤 재생성을 한 번 더 해야 한다.
`confirmed_by` 16줄은 담당자가 직접 원본을 열어 채운다.

그래서 이 기록이 담는 실측은 모두 **임시 data-root 사본**에서 돌린 것이고
커밋된 `data/gwangju/`는 건드리지 않았다.

### 고친 것 — 검증에 실패한 매핑을 `parse`까지 넘긴다

폴백 정책: "원본별 후보 수와 미해결 수를 알면 기록하고, 후보 수 자체를 모르면 `알 수 없음`으로
표시한다. 이를 0건 손실로 보고하지 않는다." 그런데 `parse.json`의 미해결 원본은
`SourceReport.candidates`가 전부 `null`이었다. `headermap`이 첫 실패에서 그 원본을 놓아
버려 `parse`에 후보를 셀 근거가 남지 않았기 때문이다.

- `HeaderMapOutput`에 `unresolved_mappings`를 둔다. 미해결 원본의 **표마다 얻은 헤더 매핑**이며
  검증에 실패한 것도, 같은 원본의 다른 표가 통과시킨 것도 함께 싣는다. `mappings`와 달리 레코드를
  내지 않는다.
- `headermap`은 표 하나가 실패해도 **남은 표를 끝까지 판정한다.** 원본의 분모는 표 하나가 아니라
  원본 전체이므로, 첫 실패에서 멈추면 여러 시트짜리 원본의 후보 수를 알 수 없다. 원본의 사유는
  예전처럼 **첫 실패**의 것이다.
- `parse`는 그 매핑으로 후보만 세고 분모에서 뺀 행의 위치·종류도 남긴다. **표 하나라도 매핑이
  없거나 후보가 0건이면 그 원본의 후보 수는 `알 수 없음`이다** — 쓰지 않기로 한 판정의 0건은
  집행 없음의 증거가 아니다(`layout=none`도 같다).
- 계약이 바뀌었으므로 `headermap` 산출물을 **v2**로 올렸다. 버전을 올리지 않으면 커밋된 v1
  `headermap.json`으로 `parse`만 다시 돌렸을 때 후보 수가 조용히 전부 `null`이 된다.
  이제 `parse`가 "rerun the producing stage"로 막는다. `run`은 `headermap`을 먼저 돌리므로
  영향이 없고, CI의 사이트 빌드는 `headermap.json`을 읽지 않는다. 커밋된 산출물 사본으로 확인했다:
  `build`는 종료 코드 0이고 `dist/`를 그대로 만들며, `parse`만 따로 돌리면 종료 코드 1과
  `cause=regeneration-required`로 멈춘다.

### 실측 — 광주 원본 173개

`GEMINI_API_KEY` 없이 임시 data-root 사본에서 돌렸다(양쪽 공통, 저장소 루트 기준).

```text
uv run python -m deliciousmap headermap --city gwangju --raw-root ../deliciousmap-raw \
  --data-root <임시>/data
uv run python -m deliciousmap parse --city gwangju --raw-root ../deliciousmap-raw \
  --data-root <임시>/data
```

| 단계 | 결과 |
| --- | --- |
| headermap | 종료 코드 0, 53초. 매핑 181 · 미해결 19(`validation_failed` 16 · `model_not_configured` 3) · 미해결 원본의 매핑 20 |
| parse | 종료 코드 0, 1분 30초. 원본 173개 보고, 레코드 2,797 · 미해결 19 |

#### 후보 663건이 #62의 전수 대조와 그대로 맞는다

코드가 센 16개의 후보 수가 [#62](issue-62.md)에서 사람이 원본별로 남긴
`data/manual/gwangju/sources.jsonl` 16줄과 **원본마다 정확히 일치**한다(합계 663).

| 원본 | 사유 | 후보(`parse.json`) | 분모에서 뺀 행 |
| --- | --- | --- | --- |
| 10905-1.xls | merchant_blank | 47 | 1 |
| 10909-1.xls | total_mismatch | 6 | 1 |
| 10922-1.xls | merchant_blank | 15 | 1 |
| 10929-1.xls | merchant_blank | 10 | 1 |
| 10933-1.xls | merchant_blank | 13 | 1 |
| 10938-1.xls | merchant_blank | 224 | 0 |
| 10939-1.xls | merchant_blank | 57 | 4 |
| 10956-1.xlsx | total_mismatch | 110 | 2 |
| 10960-1.xlsx | merchant_blank | 57 | 531 |
| 10977-1.xls | total_mismatch | 6 | 1 |
| 10990-1.xls | total_mismatch | 17 | 1 |
| 10991-1.xls | total_mismatch | 15 | 1 |
| 10999-1.xls | total_mismatch | 5 | 1 |
| 11000-1.xlsx | merchant_blank | 31 | 1 |
| 11015-1.xls | total_mismatch | 21 | 1 |
| 11017-1.xlsx | merchant_blank | 29 | 1 |
| **합계** | | **663** | **549** |

여러 시트짜리 두 원본도 시트를 모두 세었다. 10939-1은 sheet1 34 · sheet2 8 · sheet3 4 ·
sheet4 11이고, 10956-1은 sheet1 25 · sheet2 85다. #62가 "sheet2~sheet4 후보 23건",
"sheet2 후보 85건"이라고 적은 값과 같다.

뺀 행의 종류는 `blank` 530 · `total` 18 · `note` 1이다. 합계 행이 0개인 10938-1과 10960-1은
#62가 "합계 행 없음"이라고 적은 두 원본이고, 10960-1의 530줄은 그 시트 뒤에 붙은 빈 행이다.

#### PDF 3개는 후보 수도 `알 수 없음`이다

10882-1 · 10883-1 · 10943-1은 사유가 `unsupported_format`에서 **`model_not_configured`**로
바뀐다. #62가 PDF 읽기 경로를 넣었으므로 형식은 읽혔고 헤더 판정만 남았다는 뜻이다. 판정이 없으니
매핑도 없고, 후보 수는 `null`로 남는다 — 0건으로 바꾸지 않는다.

#### 레코드·예산·캐시는 그대로다

| 확인 | 결과 |
| --- | --- |
| `records.csv` | 커밋된 파일과 **바이트까지 같다**(레코드 2,797) |
| `_shared/llm-budget.jsonl` | 한 줄도 늘지 않았다. 키가 없어 호출이 없었고 누적 사용액은 **USD 0.653** 그대로다 |
| `_shared/headermap.jsonl` | 그대로. 새 판정이 없으니 서명 캐시도 늘지 않았다 |
| `gwangju/headermap-answers-v1.jsonl` | 그대로 |

"표 하나가 실패해도 남은 표를 끝까지 판정한다"는 변경은 원리상 그 표들에 모델 호출을 부를 수
있다. 이번 실측은 키가 없어 애초에 호출이 불가능했으므로 그것만으로 "호출이 늘지 않는다"고 할 수
없다. 다만 **이 변경이 새로 판정한 표 다섯(10939-1의 sheet2~sheet4, 10956-1의 sheet2)이 모두
공통 서명 캐시로 덮였다** — 키가 있었더라도 이 다섯에는 호출이 필요 없었다는 뜻이다. 캐시가 덮지
못하는 표에는 호출이 생기며, 그때는 공통 USD 15 예산이 그대로 집행을 막는다. 키가 준비된 재생성
실행에서는 이 몫까지 포함해 호출 수와 비용을 센다.

산출물 크기는 `headermap.json` 74.4 → 80.7KB, `parse.json` 45.6 → 56.8KB다. ADR-0001의 파일당
20MB 상한과 무관하다.

### 구현과 TDD

실패 → 최소 구현 → 통과를 확인한 동작(`tests/test_parse_cli.py`):

- 검증에 실패한 원본이 후보 수를 싣는다: 최초 `null`이었다.
- 한 시트가 실패해도 통과한 시트의 후보까지 함께 센다(`unresolved_mappings`에 두 시트가 온다).
- 표 하나라도 매핑이 없으면 후보 수는 `알 수 없음`이다: 카드형 답을 받은 시트가 남은 경우.
- 판정을 받지 못한 원본(`model_not_configured`)의 후보 수는 `null`로 남는다.
- 분모에서 뺀 행의 위치·종류를 미해결 원본에도 남긴다.
- 쓰지 않기로 한 판정이 후보를 하나도 찾지 못하면 0건이 아니라 `알 수 없음`이다: 열이 밀려
  지출 행의 금액 칸이 비고 합계 행만 읽히는 매핑으로 확인했다.

### 자동 검사 — 1차

| 검사 | 결과 |
| --- | --- |
| `uv run ruff check .` | 통과 |
| `uv run ruff format --check .` | 통과, 105개 파일 |
| `uv run mypy src` | 통과, 44개 소스 파일 |
| `uv run pytest` | 457 passed |
| `git diff --check` | 통과 |
| gitleaks 8.30.1 (커밋 훅, 스테이지) | no leaks found |

### 알고도 하지 않은 것

- **`parse` 자신이 미해결로 돌리는 경로에는 후보 수를 달지 않았다.** `parse`가 직접 잡는
  `validation_failed` · `unsupported_format` · `unreadable`은 `headermap`이 통과시킨 원본을
  `parse`가 다시 읽다 실패한 경우라 원본 파일이 그 사이에 바뀌지 않는 한 일어나지 않는다.
  광주 19개는 모두 `headermap`에서 왔다. 실제로 일어나는 경로가 생기면 그때 같은 셈을 붙인다.
- **미해결 16개는 그대로 16개다.** 폴백 정책의 "제출 전 미해결 0개"는 이 16개를 어떻게 할지
  정해야 채워지며, 정책을 바꾸는 일이므로 [#66](https://github.com/snowjaewon/OfficialDeliciousMap/issues/66)의
  제외 범위대로 별도로 제안한다.

## 2차 — PDF 헤더 판정 1회와 `data/gwangju/` 재생성

2026-09-13, 기반은 `develop` `08f1f82`. 1차가 남긴 셋 중 **PDF 헤더 판정과 재생성을 했고
`confirmed_by`는 그대로 남았다.**

### 실행 상한을 걸고 돌렸다

사용자 결정(2026-09-13): 이번 실행이 쓸 금액을 **USD 0.75**로 제한한다. `budget.py`의
`LIMIT_USD`는 프로젝트 누적 한도라 실행별 상한이 없으므로, 실행 동안만 `15` →
`1.40274150`(실행 전 누적 `0.65274150` + `0.75`)으로 낮췄다. `reserve`가 호출 한 건마다
`누적 + 예상상한 > LIMIT_USD`를 검사하므로 단계 내부까지 막힌다. 실행 뒤 `15`로 되돌렸고
커밋에는 코드 변경이 없다. 실제 지출은 상한의 **0.4%**였다.

### 명령

Git Bash, 저장소 루트 기준. 이 환경의 `uv 0.12.3`은 `uv run`이 `.env`를 자동으로 읽지 않아
`--env-file`이 필요하다(실제로 걸어 확인했다). `<.env>`는 키가 담긴 `.env`의 경로다.

```text
# <단계>는 headermap · parse · classify · geocode · closure · build 순서로 한 번씩
uv run --env-file <.env> python -m deliciousmap <단계> --city gwangju \
  --raw-root ../deliciousmap-raw
```

`run` 대신 단계로 나눈 것은 단계마다 예산 장부의 증분을 재기 위해서다. 산출물은 `run`과 같다.

| 단계 | 종료 코드 | 걸린 시간 |
| --- | --- | --- |
| headermap | 0 | 26초 |
| parse | 0 | 44초 |
| classify | 0 | 13초 |
| geocode | 0 | 1분 48초 |
| closure | 0 | 47초 |
| build | 0 | 1분 11초 |

### PDF 3개가 모델 호출 1회로 매핑됐다

| 항목 | 재생성 전 | 재생성 후 |
| --- | --- | --- |
| 매핑 | 181 | **184** |
| 미해결 원본 | 19 (`validation_failed` 16 · `unsupported_format` 3) | **16** (`validation_failed` 16) |
| `headermap.json` 계약 | v1 | **v2** |

모델 호출은 **1회**다. 장부에 예약·정산 각 한 줄이 쌓였고 키가
`header_mapping:6ad15d6680e5cc59:table1:…`이다. `sheet1`이 아니라 **`table1`** 인 것이 #62가
넣은 PDF 읽기 경로를 탔다는 표시다. 그 판정이 공통 서명 캐시에 쌓이자 나머지 두 PDF는 캐시로
덮였다 — `_shared/headermap.jsonl`이 286 → 287줄로 **한 줄만** 늘었다. 이슈 본문이 예측한
그대로다.

커밋된 v1 `headermap.json`과 새 계약의 어긋남(`parse`만 따로 돌리면 `regeneration-required`로
멈추던 것)도 이 재생성이 함께 풀었다.

### 레코드 2,797 → 2,810, 차이는 전부 PDF 3개다

| 원본 | 레코드 | 원본 합계 대조 | 재게시 병합 |
| --- | --- | --- | --- |
| 10882-1 (`6ad15d66…`) | 5 | `matched` | 0 |
| 10883-1 (`7da858f7…`) | 3 | `matched` | 0 |
| 10943-1 (`fc721768…`) | 5 | `matched` | 0 |
| **합계** | **13** | | **0** |

이슈 본문의 "+13(10882-1 +5 · 10883-1 +3 · 10943-1 +5)" 예측과 원본마다 같고, 세 원본 모두
합계 행과 항목 합이 맞는다.

[#63](https://github.com/snowjaewon/OfficialDeliciousMap/issues/63) 댓글이 물은 "PDF 원본이 실은
지출이 다른 원본과도 겹치면 더 줄 수 있다"는 **일어나지 않았다.** 세 원본의 `repeated`가 모두
0이고 `parse.json`의 `repeated_expenses`가 재생성 전후로 한 글자도 바뀌지 않았다: 합친
117묶음·122건 · 사람이 확정한 2묶음·2건 · 가른 0 · 남긴 0. 그래서 차이는 `+13` 그대로다.

가를 근거가 없어 남긴 묶음은 **0**이다(`unmerged_expenses` 0). 사람이 확정한 2묶음은
[#69](https://github.com/snowjaewon/OfficialDeliciousMap/issues/69)의 경로로 이미 들어온 것이며
이번 실행이 늘리지 않았다.

#### 이슈가 적어 둔 예상치와 잇는다

이슈는 본문에 **2,934**, 댓글에 **2,812**를 적고 "숫자를 미리 확정하지 말 것 · 실행에서 실제 수를
재고 그 차이의 출처를 적는 것이 맞다"고 했다. 실제 값은 **2,810**이다. 커밋된 `records.csv`의
이력으로 그 차이를 잇는다.

| 커밋 | 레코드 | 무엇이 줄이거나 늘렸나 |
| --- | --- | --- |
| `eea3ee2` (#51) | 2,921 | 재게시 병합 전의 광주 산출물 |
| `b427295` (#64) | 2,799 | 재게시 병합 **−122** |
| `b7646b0` (#69) | 2,797 | 사람이 확정한 병합 **−2** |
| `07d8ce2` (이번) | **2,810** | PDF 3개 **+13** |

- 본문의 **2,934**는 `2,921 + 13`이다. 재게시 병합이 들어오기 전의 예측이다.
- 댓글의 **2,812**는 `2,799 + 13`이다. 그 댓글을 쓸 때 커밋된 값이 2,799였고, 그 뒤 #69가 사람이
  확정한 병합 2건을 더 합쳐 **2,797**이 됐다. 댓글이 "커밋된 광주 산출물(레코드 2,799)"이라고
  적은 것은 그 시점의 값이다.
- 그래서 **2,810 = 2,797 + 13**이고, 댓글의 2,812와의 차이 **2**는 PDF와 무관하게 #69가 먼저
  줄인 몫이다. `2,921 − 122 − 2 + 13 = 2,810`으로도 같은 값이 나온다.

### 후보 663건이 커밋된 장부에 실렸다

1차가 임시 data-root에서만 확인한 값이 이제 커밋된 `parse.json`에 들어갔다.

| 항목 | 재생성 전(커밋본) | 재생성 후 |
| --- | --- | --- |
| 대상 원본 | 173 | 173 |
| 읽힌 원본 | 154 | **157** |
| 미해결 원본 | 19 | **16** |
| 후보 합계 | 0 — 19개 전부 `알 수 없음` | **663** — `알 수 없음` 0 |

미해결 16개의 후보 수는 1차 표와 원본마다 같고 사유는 모두 `validation_failed`다. `알 수 없음`이
0이 된 것은 그 자리에 있던 PDF 3개가 읽혔기 때문이지, 미해결 원본의 후보를 0건으로 바꾼 것이
**아니다**.

### 나머지 단계

| 산출물 | 재생성 전 | 재생성 후 |
| --- | --- | --- |
| `records.csv` | 2,797 | **2,810** |
| `classify.json` | 식당 2,223 · 비식당 191 · 판단 보류 383 | 식당 **2,232** · 비식당 191 · 판단 보류 **387** |
| `geocode.json` | 2,223건 — 성공 296 · 실패 1,927 | **2,232건** — 성공 **299** · 실패 1,933 |
| `closure.json` | 12건 전부 `unknown` | 판정 그대로 |
| `build.json` | 레코드 2,797 · 마커 12 | 레코드 **2,810** · 마커 **12** |

레코드는 13개 늘었는데 좌표를 찾을 대상은 9개만 늘었다. 새 13건의 판정이 **식당 9 · 판단 보류
4**로 갈렸기 때문이다. 판단 보류 4건은 마커가 되지 못하고 장부에만 남는다 — 0건 손실이 아니라
사람이 확인할 몫으로 남은 것이다. 비식당은 하나도 늘지 않았다.

그 식당 9건 중 좌표를 얻은 것은 **3건**이고 6건은 실패로 남았다. 광주 전체의 성공률이 13%대라
새 9건도 그 범위 안이다.

마커가 12로 그대로인 것은 좌표를 얻은 299건이 고유 업체 **12곳**으로 묶이고 새 3건도 이미 있던
업체에 속하기 때문이다. 업체가 늘지 않았으니 마커도 늘지 않는다.

`closure.json`은 판정 12건이 글자까지 같고 `dependencies`의 앞 단계 해시만 바뀌었다. 앞 단계
산출물이 바뀌었으니 당연한 변화이며, 폐업 조회 결과가 달라진 것은 아니다.

조회 캐시 `geocode-lookup-v1.jsonl`은 1,069 → 1,071줄(+2)이고 둘 다 네이버 조회
(`naver/naver-local-1 ok candidates=1`)다. 인허가 조회는 `DATA_GO_KR_KEY`가 비어 있어 이번에도
돌지 않았다. 커밋된 캐시 1,069줄도 후보가 전부 네이버라 경로가 달라지지 않았다.

### 판정 이력은 활성 조각에만 쌓였다

[ADR-0003](../adr/0003-narrow-geocode-history-key.md)의 조각 분할대로 앞 조각은 다시 쓰이지
않았다. 상한은 `storage.py`의 `SIZE_LIMIT = 20_000_000`바이트이고 ADR-0001을 따른다.

| 이력 조각 | 재생성 전 | 재생성 후 | 상한 대비 |
| --- | --- | --- | --- |
| `…v2.jsonl` (봉인) | 15,477,093바이트 · 6,975줄 | **바이트까지 그대로** | 77.4% |
| `…v2.002.jsonl` (활성) | 16,828,919바이트 · 6,775줄 | **16,855,988바이트 · 6,784줄** | **84.3%** |

활성 조각은 9줄 27,069바이트 늘었고 상한까지 3,144,012바이트가 남았다.

활성 조각이 상한의 84%에 있지만 `append_cache_entries`가 배치가 들어가지 않으면 그 조각을 닫고
새 조각을 여므로 사람이 미리 나눌 일은 없다. 다만 다음 재생성에서 `…v2.003.jsonl`이 생길 수
있다는 것은 알아 둔다.

### 호출 수와 비용

| 용도 | 호출 | 알린 사용량 | 비용 |
| --- | --- | --- | --- |
| `header_mapping` | 1 | in=1,142 · out=71 | USD 0.0022455 |
| `classification` | 1 | in=209 · out=59 | USD 0.000756 |
| 상호 복원 비교 | **0** | — | — |
| **합계** | **2** | | **USD 0.00300150** |

누적 사용액은 **USD 0.65274150 → 0.65574300**이고 공통 한도 USD 15의 4.4%다.

#### 장부의 옛 줄은 절반 단가로 계산돼 있다

실행 전에 드러난 사실을 적어 둔다. 요금 페이지에서 확인한 `gemini-3.6-flash`의 유료 등급 단가는
1M 토큰당 입력 **1.50** · 출력 **7.50** USD다. 그런데 장부의 옛 정산 줄을 역산하면 입력
**0.75** · 출력 **3.75**로 계산돼 있다 — `in=1577 out=74`의 정산액 `0.00146025`는 0.75/3.75의
값이고, 1.50/7.50이면 `0.0029205`다. [#51 검증 기록](issue-51.md)이 적은 "1M 토큰당 입력 0.75 ·
출력 3.75 USD"가 그 출처다.

그래서 누적 **USD 0.653**은 **절반 단가로 기록된 값**이고, 현행 단가로 같은 사용량을 다시 세면
약 **USD 1.31**이다.

사용자 결정(2026-09-13): **옛 줄은 그대로 둔다.** 그때 적용된 단가의 기록이므로 손대지 않고
이번 실행부터 1.50/7.50으로 쌓는다. 실제로 이번 두 줄은 현행 단가로 계산됐다
(`in=1142 out=71` → 1,142×1.50 + 71×7.50, 1M 토큰당 = `0.0022455`).

이 어긋남이 공통 한도에 뜻하는 것: **장부가 세는 누적액이 실제 집행액보다 작을 수 있다.** 한도
USD 15까지 여유가 커서 이번 실행을 막지 않았지만, 한도에 가까워지면 이 차이를 먼저 정리해야
한다.

### 개인정보와 새 부서값

새로 들어온 13개 레코드의 칸은 기존과 같고(`record_id`·`spent_on`·`organization`·`department`·
`merchant`·`purpose`·`amount_krw`·`source_hash`·`source_location`·`repeats`) 참석자 이름·인원
칸은 없다. 목적 문구도 "(교)현안업무 협의를 위한 집행부 관계관과의 간담회" 같은 형태로 사람
이름을 담지 않는다.

`department`에 `교육문화전문위원`이 새로 들어왔다. 광주 레코드의 부서 67종 가운데 `전문위원`으로
끝나는 유일한 값이고 이 PDF 3개에서만 나온다. 기관은 `gwangju-city` 하나 그대로이며 시의회 쪽
집행이 이 원본으로 들어온 것이다. 사람 이름이 아니라 직위·기구 이름이므로 개인정보가 아니다.

## 자동 검사 — 2차

| 검사 | 결과 |
| --- | --- |
| `uv run ruff check .` | 통과 |
| `uv run ruff format --check .` | 통과, 107개 파일 |
| `uv run mypy src` | 통과, 44개 소스 파일 |
| `uv run pytest` | 457 passed (64초) |
| `git diff --check` | 통과 |
| gitleaks 8.30.1 (커밋 훅, 스테이지) | no leaks found |

코드는 한 줄도 바뀌지 않았다. 2차가 바꾼 것은 `data/` 산출물과 이 문서뿐이며, 실행 동안 낮췄던
`LIMIT_USD`는 `15`로 되돌렸다.

## 남은 일 — 이슈 #66의 나머지

`confirmed_by` 16줄 하나가 남았다. 담당자가 `data/manual/gwangju/sources.jsonl`의 16개 원본을
열어 대조하고 그 칸을 채운다. 결함 행 위치와 근거는 줄마다 이미 있고 원본은 raw-root의
`gwangju/gwangju-city/expenses/`에 있다. #62의 전수 대조는 코드가 후보 663건을 훑은 결과이며
폴백 정책이 요구하는 사람 확인은 아니다.

폴백 정책의 "제출 전 미해결 0개"는 미해결 16개를 어떻게 할지 정해야 채워진다. 정책을 바꾸는
일이므로 이슈 #66의 제외 범위대로 별도로 제안한다.
