# 이슈 #51 광주광역시청 검증 기록

1부는 게시판 정찰과 `fetch` 구현, 2부는 `headermap` 이후 단계의 실행 기록이다.
두 부는 다른 시점의 실행이며, 2부의 수치는 1부의 전량 수집 **이전** 것이다(2부 첫머리 참고).

## 1부 · 게시판 정찰과 fetch 구현

2026-09-11, Windows 11 · Git Bash · Python 3.12.10 · curl 8.x.
범위: [한 도시의 실제 수집·파싱으로 정제 산출물과 build 확인 #51](https://github.com/snowjaewon/OfficialDeliciousMap/issues/51)
가운데 **광주광역시청 1개 기관, 1개 게시판의 정찰과 `fetch` 구현**까지다.
5개 자치구와 나머지 6개 도시는 이 문서의 범위가 아니다. `headermap` 이후 단계는 2부에 있다.

### 실측과 인용의 구분

이 문서의 **실측**은 모두 2026-09-11에 이 세션에서 직접 요청해 확인한 값이다.
**인용**은 목업 저장소(`snowjaewon/governdeliciousmap`)의 코드·주석에서 읽은 값이며
이번에 재검증하지 않았다. 레지스트리에는 실측한 값만 선언했다.

### 게시판을 찾은 경로

목업 저장소에는 6개 도시의 `docs/<도시>_수집_정찰.md`가 있으나 **광주 정찰 문서만 없다**(실측:
`gh api repos/snowjaewon/governdeliciousmap/git/trees/HEAD?recursive=1`). 그래서 코드가 유일한 출처였다.
`main.py`의 `SCRAPERS`가 가리키는 `scrapers/gwangju_city.py` 모듈 상단에 게시판 주소가 박혀 있었고,
그 값을 출발점으로 삼아 아래를 전부 curl로 다시 확인했다.

### 실측 결과

| 항목 | 실측값 | 확인 방법 |
| --- | --- | --- |
| 시청 사이트 | `https://www.gwangju.go.kr/` → 302 → `/main.do` | `curl -I` |
| 사이트 이름 | `전남광주통합특별시(구)광주광역시` | `<title>`·`og:site_name`·`og:title` |
| 기관 주소·저작권 | `[61945] 전남광주통합특별시 서구 내방로 111(치평동)`, `Copyright ⓒ 2020 Jeonnam-Gwangju Special Metropolitan City` | 메인 푸터 |
| 게시판 목록 | `boardList.do?boardId=BD_0000000252&recordCnt=100` | HTTP 200, 게시글 100건 |
| 게시판 내용 | 업무추진비 집행·사용내역 공개 게시판 | 게시글 제목 |
| 게시글 수 | 10,476건 / `recordCnt=100` 기준 105쪽 | 목록의 `전체게시글 : 10,476 / 전체페이지 : 105` |
| 게시 기간 | 2004년 ~ 2026년 2분기 | 1쪽 최신·105쪽 최하단 |
| 게시글 식별 | `boardView.do?...&seq=<연번>` (최대 11024) | 목록 앵커 |
| 게시글 본문 | `boardView.do?boardId=<boardId>&seq=<seq>` 축약형으로 HTTP 200 | 8건 직접 요청(11024·11023·11021·11018·11015·9000·5000·1) |
| 페이징 | `movePage=1`·`2`의 md5가 다르고 `seq`가 겹치지 않음 | `md5sum`, 1쪽 11024~11015 / 2쪽 11014~11005 |
| 첨부 주소 | `fileDownload.do?fileSe=BB&fileKey=<boardId>|<seq>&fileSn=<연번>&boardId=<boardId>&seq=<seq>` | 목록·본문 앵커 |
| 첨부 형식 분포 | 전 범위 표본 80건에서 `.xls` 47 · `.xlsx` 27 · `.hwp` 1, 첨부 없는 글 6건 | `seq` 1~11024를 균등 표본으로 본문 요청 |
| `.hwp` | seq 7396 `…(2020. 10월~12월).hwp` → `d0cf11e0a1b11ae1`(OLE2), 14,848바이트 | 직접 내려받아 `xxd` |
| `.pdf` | seq 10943 `2026.3.시책업무추진비 사용 내역….pdf` → `%PDF-1.7`, 49,832바이트 | 직접 내려받아 `xxd` |
| `.hwpx` | seq 10681 `…주요참석자 명단.hwpx` → `504b0304`(ZIP), 74,605바이트 | 직접 내려받아 `xxd` |
| 묶음 내려받기 | 첨부가 둘 이상인 글에만 `fileDownload.do?action=zip…` 링크가 하나 더 붙고 `fileSn`이 없다 | seq 3071 본문 |
| 첨부 형식 | `.xls` → `d0cf11e0a1b11ae1`(OLE2), `.xlsx` → `504b0304`(ZIP) | `head -c 32 \| xxd`, 3건 |
| 확장자 대조 | 내려받은 5건 모두 확장자와 매직 바이트가 일치 | 위와 같음 |
| Referer | 없어도 동일한 원본(43,520바이트, 동일 매직) | Referer 유·무 2회 요청 |
| User-Agent | 비워도, 프로젝트 UA로도 HTTP 200 | UA 3종 비교 |
| 응답 인코딩 | UTF-8 (`Content-Type: text/html;charset=UTF-8`) | 응답 헤더 |
| 폐기 파라미터 | `pageId=www101`을 붙이면 302 → `/siiru.do` | HTTP 302 |

정찰로 내려받은 원본 3건은 저장소 밖 임시 디렉터리에만 두었고 커밋하지 않았다.

### 목업에서 인용한 값(재검증 안 함)

- `scrapers/gwangju_city.py`가 `max_pages` 기본 5쪽, 쪽당 100건으로 수집했다는 것.
- `cities.py`가 광주 자치구를 동구·서구·남구·북구·광산구 5곳, 중심 좌표를 `(35.1595, 126.8526)`,
  인허가 코드를 `동구 5805000` 외 4건으로 선언했다는 것.
- 주소 표기가 `전남광주통합특별시 …`로 바뀌고 곡성·해남군 등을 포함한다는 주석.

### 지뢰 세 가지의 처리

**지뢰 1 — 광주 정찰 문서 부재**: 사실이었다. 코드에서 출발해 위 실측으로 전부 다시 확인했다.

**지뢰 2 — 전남광주통합특별시**: **사실이다.** 시청 사이트가 스스로를 `전남광주통합특별시(구)광주광역시`로
표기하고, 별도 도메인 `www.jeonnam-gwangju.go.kr`(`<title>전남광주통합특별시`)가 살아 있다.
그 사이트에는 광주 5개 구와 전남 22개 시군(목포·여수·순천·나주·광양·담양·곡성·구례·고흥·보성·화순·
장흥·강진·해남·영암·무안·함평·영광·장성·완도·진도·신안)이 모두 등장한다.

따라서 브리프의 지적대로 **기관 범위와 `MapBounds`가 둘 다 현재 선언과 어긋난다**:

- `registry/gwangju.py`의 `MapBounds(35.0362, 126.6495, 35.2588, 127.0228)`는 옛 광주광역시 경계이며
  통합특별시를 담지 못한다.
- 목업이 전제한 "시청 + 자치구 5곳"도 통합 후 기관 범위와 다르다.
- `CONTEXT.md`의 도시 정의("특별시 1곳과 광역시 6곳")와 `City.name`(`광주`)도 영향을 받는다.

이 둘은 도시 하나의 수집 범위를 넘어 **도시 정의 자체를 다시 정하는 일**이라 이 이슈에서 고치지 않았다.
값은 그대로 두고 근거만 남겼으며, 결정은 별도로 올린다. 이번 수집 대상은 실측한 시청 게시판 1곳뿐이다.

**지뢰 3 — 첨부 판정**: 광주는 Referer 없이도 정상 원본을 준다(실측). 다만 규칙은 기관에 기대지 않고
매직 바이트로 세웠다. `boards.require_original`이 OLE2·ZIP·PDF 서명을 확인하고 게시판이 밝힌 확장자와
대조해, 200으로 온 HTML이나 확장자와 어긋나는 내용은 저장하지 않고 `unsupported-format`으로 실패한다.
컨테이너 표는 기관 공통이지만 **받아들일 확장자는 기관마다 실측한 것만** 선언한다(광주는 `.xls`·`.xlsx`).

### 이번에 구현한 것

| 범위 | 결과 |
| --- | --- |
| 레지스트리 | `gwangju-city`와 `expenses` 게시판을 실측 주소·실측 이름으로 선언 |
| 게시판 경계 | `boards.py` — 첨부 참조 계약, 앵커·본문만 읽는 HTML 해석, 컨테이너 판정, 응답 상한, 요청 간격 |
| 스크래퍼 | `scrapers/gwangju.py` — 실측 구조대로 목록·본문·첨부를 해석 |
| 수집 | `collection.py` — 원본을 `Paths.original` 경로에 저장하고 `SourceRef`로 출처를 남김 |
| 재개 | 게시글 단위 추가형 기록 `collected.jsonl`로 중단한 수집을 이어서 함 |
| 어댑터 | `LocalAdapters.fetch`가 `not-implemented` 대신 실제 수집을 실행 |
| 요청 경계 | `HttpTransport(limit=..., interval=...)`와 `ExecutionContext.board_transport` |

기관 이름은 이 기관이 스스로 밝히는 `전남광주통합특별시(구)광주광역시`를 그대로 쓴다. 통합 전 이름
`광주광역시청`은 실측 표기가 아니어서 선언하지 않았다. `…특별시청` 같은 조어도 만들지 않았다.

원본 저장 이름은 `<seq>-<fileSn><확장자>`다. 게시판이 준 파일명은 경로로 쓰지 않는다.
목록에 실린 첨부 링크의 `seq`로 첨부가 달린 게시글만 가려내므로, 첨부 없는 게시글의 본문은 열지 않는다.
첨부 형식은 이 게시판에서 실측한 `.xls`·`.xlsx`·`.hwp`·`.hwpx`·`.pdf`만 받는다. 그 밖의 형식은 컨테이너가 온전하더라도
`unsupported-format`으로 알린다. 실패 사유는 셋으로 나눈다: 형식 문제는 `unsupported-format`,
닿지 못함은 `service-unavailable`, 실측한 구조와 다르거나 통째로 읽을 수 없는 크기는 `adapter-failed`다.
응답 상한 20MB는 실측 표본(28KB~126KB)보다 두 자리 여유를 둔 값이며, 정제 산출물의 20MB 상한
(ADR-0001)과는 정하는 이유가 다른 별개의 값이다.

### 구현과 TDD

테스트 경계는 공개 CLI(`cli.main`)다. 새로 추가한 `board_transport`에만 응답을 주입하고
스크래퍼·판정·저장은 그대로 실행한다. 네트워크는 차단되고 원본은 임시 디렉터리에만 쓴다.
fixture(`tests/fixtures/gwangju/`)는 실측한 마크업 구조를 따르되 부서명을 합성값으로 바꾼 축약본이며,
첨부는 실측한 매직 바이트로 시작하는 합성 바이트다.

실패 → 최소 구현 → 통과를 확인한 동작(`tests/test_gwangju_fetch.py` 16건):

- 레지스트리가 실측한 시청 게시판을 선언: 최초 기관·게시판 부재로 `invalid organization selection`.
- 게시판 스크래퍼 모듈을 레지스트리보다 먼저 불러도 순환하지 않음: 최초 `partially initialized module`로 실패.
- 두 쪽에 걸친 첨부 3건을 저장소 밖에 저장하고 이름을 `<seq>-<fileSn><확장자>`로 붙임: 최초 `not-implemented`.
- 원본마다 그 게시글 주소를 출처로 남기고 내용 해시로 구별: 최초 `SourceRef` 부재로 실패.
- 목록이 밝힌 전체 페이지를 모두 훑고, 첨부가 없는 게시글의 본문은 요청하지 않음: 최초 페이징 부재.
- 이미 받은 원본은 다시 내려받지 않음: 최초 매 실행 재다운로드로 실패.
- 원본 컨테이너가 아닌 응답(200으로 온 HTML)을 저장하지 않고 `unsupported-format`: 최초 판정 부재.
- 확장자와 매직 바이트가 어긋나는 첨부를 거부: 최초 확장자만 믿어 실패.
- 게시판에 닿지 못하면 0건이 아니라 `service-unavailable`로 실패: 최초 `io-error`로 뭉개져 실패.
- 수집 보류 기관은 요청하지 않고 사유를 `empty_reason`에 남기며 종료 0: 최초 보류 처리 부재.
- 보류 사유는 `CONTEXT.md`의 네 가지만 선언 가능: 기존 계약 회귀 확인.
- 기관 이름이 실측한 자기 표기와 일치: 최초 통합 전 이름을 선언해 실패.
- 이 게시판에서 실측하지 않은 형식(내용까지 온전한 OOXML인 `.hwpx`)을 거부: 최초 공통 컨테이너 표만 보고 통과해 실패.
- 실측한 네 형식(`.xls`·`.xlsx`·`.hwp`·`.pdf`)을 모두 받아들임: 최초 두 형식만 선언해 실제 수집이 `.pdf`에서 멈춰 실패.
- 묶음 내려받기 링크(`action=zip`, `fileSn` 없음)를 첨부로 세지 않음: seq 3071 실측 구조를 fixture로 고정.
- 목록이 전체 페이지 수를 잃으면 `adapter-failed`: 최초 `invalid-artifact`로 산출물 문제처럼 보고해 실패.
- 통째로 읽을 수 없는 크기의 응답을 `adapter-failed`: 최초 `unsupported-format`으로 사유가 뒤바뀌어 실패.
- `boardId` 없는 게시판 선언을 만들 때 거부: 최초 수집 도중 `KeyError`가 나 실패.
- 한 기관에 연달아 보내는 요청 사이에 간격을 둠: 최초 간격 없이 몰아쳐 실패.
- 다시 실행하면 끝낸 게시글의 본문도 첨부도 요청하지 않고, 출처는 수집 기록 전체를 실음:
  최초 매 실행 본문 1만 건을 다시 열어 실패.
- 첨부를 다 받기 전에 멈춘 게시글은 기록하지 않아 다음 실행이 다시 받음: 최초 부분 수집이 완료로 남아 실패.

기존 `tests/test_cli.py`의 "`fetch`는 `not-implemented`" 두 건은 그대로 통과한다. 게시판을 아직
선언하지 않은 도시는 0건 성공이 아니라 `not-implemented`로 실패한다. 미수집 도시를 완료로 표시하지 않기 위해서다.

실측 HTML·첨부로도 해석을 확인했다(수집은 하지 않음). 목록 1쪽 100건 중 99건에서 첨부를 찾고,
전체 페이지 105를 읽고, 실측 첨부 3건은 `require_original`을 통과하고 시청 메인 HTML은 거부된다.

### 첫 운영 수집

2026-09-11 `uv run python -m deliciousmap fetch --city gwangju --raw-root ../deliciousmap-raw`를 실행했다.

수집은 실측하지 않은 형식을 만날 때마다 멈췄고, 그때마다 그 형식을 내려받아 매직 바이트를 확인하고
선언한 뒤 이어서 받았다. 멈춘 시점까지의 수집은 `collected.jsonl`에 남아 그대로 재사용했다.

| 멈춘 지점 | 원인 | 실측하고 선언한 것 |
| --- | --- | --- |
| 게시글 80건 | seq 10943의 `.pdf` | 전 범위 표본 80건으로 형식 분포 재실측, `.hwp`·`.pdf` 추가 |
| 게시글 332건 | seq 10681의 `.hwpx` | `.hwpx` 추가 |
| 게시글 2,302건 | seq 8621의 첨부가 **404** | 유실 원본을 서비스 장애와 구별해 `missing`에 남기고 계속 진행 |
| 게시글 2,584건 | seq 8316에서 일시적 실패 (곧바로 다시 200) | 일시적 실패만 4회까지 2초 배수로 재시도 |
| 게시글 4,722건 | seq 5962의 `.xls`가 실제로는 **Excel 2003 XML(SpreadsheetML)** | 컨테이너에 SpreadsheetML 추가, 그리고 **멈추는 방식 자체를 고침** |
| 게시글 8,163건 | seq 2148의 첨부가 **0바이트**(HTTP 200) | 404와 같은 부류로 보고 `missing`에 `empty`로 남김 |
| 게시글 10,233건(끝까지) | 보류 7건을 한 번에 보고: `.xlsm` 5건, 이름이 `.xls`인 OOXML 2건 | `.xlsm` 선언, 확장자 불일치는 **거부하지 않고 판정한 컨테이너를 출처에 기록** |

정찰 표본 8건에는 `.xls`·`.xlsx`만 있었다. 표본이 작으면 드문 형식을 놓친다는 것이 이 과정에서 드러났고,
멈춤이 그것을 숨기지 않고 알렸다.

다섯 번째 멈춤에서 **멈추는 방식 자체를 바꿨다.** 22년치 게시판은 드문 형식이 뒤늦게 나오므로
하나 만날 때마다 그 자리에서 멈추면 그만큼 다시 훑어야 한다. 지금은 게시판을 끝까지 훑고,
실측하지 않은 형식을 만난 첨부를 모아 `unmeasured.jsonl`(저장소 밖)에 적은 뒤 한 번에
`unsupported-format`으로 알린다. 그 게시글은 기록하지 않으므로 형식을 선언하고 다시 실행하면
그때 받는다. 성공으로 넘어가지 않는다는 점은 그대로다.

`.xls`가 OLE2와 SpreadsheetML 둘 다로 올라오는 것도 이때 드러났다. 서명이 `<?xml`뿐이면 다른 XML과
갈리지 않으므로 앞부분 4KB에서 `urn:schemas-microsoft-com:office:spreadsheet` 표식을 함께 확인한다.

**확장자와 내용이 어긋나면 거부한다**는 처음 판단은 실측으로 뒤집혔다. 끝까지 훑어 모은 보류 7건 중
2건(seq 963·857)은 이름이 `.xls`인데 내용이 OOXML인 **진짜 원본**이었다. 거부하면 그 지출을 잃는다.
그래서 판정한 컨테이너를 `SourceRef.container`에 남기고 저장 이름은 게시판이 밝힌 대로 둔다.
헤더 매핑·파싱은 확장자가 아니라 이 값을 봐야 한다. 나머지 5건은 `.xlsm`(OOXML)이라 선언에 더했다.

세 번째와 여섯 번째 멈춤은 형식이 아니라 **기관 쪽에 원본이 없는 경우**였다. seq 8621의 본문은
`2022년 2분기 시책추진업무추진비(물순환정책과).xlsx`를 링크하지만 그 주소는 404와 오류 HTML을 준다
(목록·본문은 정상 200이므로 차단이 아니다). 다시 요청해도 같으므로 `service-unavailable`로 수집을
멈추는 것은 틀렸고, 그렇다고 조용히 건너뛰면 장부에서 사라진다. 그래서 `fetch.json`의 `missing`에
기관·게시판·게시글 주소·게시판이 밝힌 파일 이름·사유를 남기고 수집을 이어 간다.

같은 부류가 하나 더 있다. seq 2148은 200으로 **0바이트**를 준다. 응답은 정상이지만 받을 것이 없다.
사유를 `gone`(404)과 `empty`(200·내용 없음)로 나눠 남긴다. 둘 다 다시 묻지 않는다.

첨부 가운데 `주요참석자 명단.hwpx`처럼 업무추진비 내역이 아닌 파일도 있다. 개인정보는 파싱 단계에서
버린다([SECURITY.md](../../SECURITY.md)). 원본 자체는 저장소 밖에만 둔다.

### 수집 결과

2026-09-11 전량 수집을 마쳤다(종료 코드 0). 게시판 전체 10,476개 게시글 중 첨부가 있는
**10,240개** 게시글에서 원본 **11,035개**를 받았다. 나머지 236개는 첨부가 없는 게시글이다.

| 항목 | 값 |
| --- | --- |
| 게시글 | 10,240개 (첨부 2개 이상인 글 574개) |
| 원본 | 11,035개, 합계 693.8 MB, 평균 64.4 KB |
| 컨테이너 | `ole2` 6,199 · `ooxml` 4,762 · `pdf` 71 · `spreadsheetml` 3 |
| 확장자 | `.xls` 6,092 · `.xlsx` 4,754 · `.hwp` 112 · `.pdf` 71 · `.xlsm` 5 · `.hwpx` 1 |
| 받지 못한 원본 | 101개 (`gone` 100 · `empty` 1) |
| `fetch.json` | 3.89 MB |

**확장자와 컨테이너가 어긋나는 원본이 5개 있다.** `.xls`로 올라왔지만 내용은 OOXML 2개,
SpreadsheetML 3개다. 헤더 매핑·파싱은 확장자가 아니라 `SourceRef.container`를 봐야 한다.

받지 못한 101개는 **초기 게시글에 몰려 있다.** `seq` 1~1000 구간에 92개이고 나머지 구간은
1001~3000에 1개, 3001~8000에 5개, 8001~11024에 3개다. 2004~2009년 글의 첨부가 기관 쪽에서
사라진 것으로 보인다. 그 연도의 장부는 그만큼 비어 있으며, 무엇이 비었는지는 `fetch.json`의
`missing`에 게시글 주소·파일 이름과 함께 남아 있다.

원본은 저장소 밖(`--raw-root`)에만 있고 커밋하지 않았다. 커밋한 것은 `data/gwangju/fetch.json` 하나다.

### 검사

```text
uv run ruff check .          통과
uv run ruff format --check . 통과
uv run pytest                207건 통과
uv run mypy src              통과 (tests의 기존 오류 54건은 이번 변경과 무관)
git diff --check             통과
```

### 남은 제한 — #51의 완료 기준 대비

이 작업은 #51을 닫지 못한다. 남은 것은 다음과 같다.

- 목록은 매번 다시 훑고, 이미 끝낸 게시글에 나중에 첨부가 더 붙으면 알아차리지 못한다.
- 2004~2009년 글의 첨부 92개가 기관 쪽에 없다. 그 연도의 장부는 그만큼 비며 되살릴 방법이 없다.
- `headermap`·`parse`·`classify` 어댑터는 그대로 `not-implemented`다. `data/gwangju/` 정제 산출물,
  레코드·마커 수, `markers.json`/`records.json` 크기, 로컬 `build` 확인은 모두 아직 없다.
- OLE2는 `.xls`와 `.hwp`가, ZIP은 `.xlsx`와 `.hwpx`가 공유한다. 컨테이너 안을 열어 둘을 가르는 일은
  `headermap`·`parse`의 몫이다.
- 목록에 첨부 링크가 없는 게시글은 본문을 열지 않는다(실측: 1쪽 100건 중 99건에 링크가 있다).
  목록 markup이 바뀌어 링크가 사라지면 수집이 0건이 되고, 그 사실은 `empty_reason`에만 남는다.
- 광주 5개 자치구는 정찰하지 않아 선언하지 않았다. 수집 보류(`hold_reason`)가 아니라 미정찰이다.
- 통합특별시에 따른 도시 이름·경계·기관 범위는 위 지뢰 2대로 미결이며, 아직 별도 이슈를 열지 않았다.
  이 문서가 그 결정의 근거이고, 이슈를 열기 전까지 광주를 "수집 완료한 도시"로 표시하지 않는다.

## 2부 · headermap·parse·classify·build 실행 기록

> **이 부의 수치는 낡았다.** 아래 실행은 2026-01-01 이후 원본 251개만 받은 `fetch` 위에서 했다.
> 1부의 전량 수집(원본 10,978개·유실 101건)이 `develop`에 먼저 들어가면서 `fetch.json`이 바뀌었고,
> `data/gwangju/`의 `headermap` 이후 산출물은 그 입력과 더는 맞지 않는다. 다시 실행해야 하며,
> 그때의 LLM 비용은 [폴백 정책](../specs/header-mapping-fallback.md)의 공통 예산으로 다시 따져야 한다.

2026-09-11, Windows 11 · Git Bash · Python 3.12.10 · Chrome.
범위: [한 도시의 실제 수집·파싱으로 정제 산출물과 build 확인 #51](https://github.com/snowjaewon/OfficialDeliciousMap/issues/51).
사용법은 [README의 CLI](../../README.md#cli)에 있다.

### 선택한 도시·기관

**광주 · 광주광역시청(`gwangju-city`)** 하나다. [#13](https://github.com/snowjaewon/OfficialDeliciousMap/issues/13)의
레인 1 첫 도시이고, 도시 안에서는 시청을 먼저 한다. 게시판은
`https://www.gwangju.go.kr/boardList.do?boardId=BD_0000000252`이다. 2026-09-11 목록·게시글·원본 다운로드가
열리는 것을 확인했고, 2026년 상반기 게시글이 있었다. 5개 자치구는 게시판을 확인하지 않아 레지스트리에
선언하지 않았다. **광주는 #13의 도시 완료 기준(정규 기관 전부)을 아직 충족하지 않는다.**

수집 보류 기관은 없다. 화면의 자료 범위에는 선언한 한 기관만 `수집 완료`로 나온다.

### 실행

저장소 루트, Git Bash. 원본은 기본 `--raw-root`인 `../deliciousmap-raw`에 두었다.

```text
uv run --env-file .env python -m deliciousmap run --city gwangju
uv run python -m http.server 8765 --directory dist --bind 127.0.0.1
```

`run`은 코드 커밋 `b9e5581`에서 **종료 코드 0**으로 끝까지 통과했다(5분 47초). 앞선 단계별 실행에서 받은
원본·모델 답·판별 캐시·네이버 조회를 재사용했다. 그 뒤 중간 실행의 낡은 판정이 섞인
`geocode-history-v2.jsonl`만 지우고 `geocode`·`closure`·`build`를 다시 실행했다(외부 호출 없음, 아래 참고).

### 결과

| 단계 | 결과 |
| --- | --- |
| fetch | 게시글 247개, 원본 251개(xls 155 · xlsx 88 · PDF 6 · ZIP 2). 게시일 2026-01-01 이후 |
| headermap | 원본 222개의 표 매핑, 원본 29개 미해결(아래). 헤더 서명 캐시는 키 25개 |
| parse | 원본 222개 통과. 지출 후보 6,193건 중 **기간 안 레코드 2,921건**, 기간 밖 3,272건, 제외 행 141개, 0원·음수 0건 |
| 합계 대조 | 통과 원본 중 일치 168 · 합계 없음 45 · 대조 불가 9 |
| classify | 식당 2,325 · 비식당 193 · 판단 보류 403(모두 모델이 모호하다고 판정, 호출 실패 0) |
| geocode | 식당 레코드 2,325건 중 `missing_address` 2,055 · `no_candidates` 270 |
| closure | 대상 0 |
| build | **레코드 2,921 · 마커 0** |

| 공개 파일 | 크기 |
| --- | --- |
| `dist/gwangju/markers.json` | 69바이트 |
| `dist/gwangju/records.json` | 1,140,080바이트 |

월별 레코드는 1월 885 · 2월 703 · 3월 692 · 4월 307 · 5월 196 · 6월 138건이다. 게시판의 마지막 게시글이
2026-06-29여서 2분기가 적다. 6월 집행분이 통합특별시 출범 뒤 다른 게시판에 올라왔는지는 확인하지 않았다.

**마커가 0인 이유.** 광주시청 원본에는 상호만 있고 주소가 없다.
[#33](https://github.com/snowjaewon/OfficialDeliciousMap/issues/33#issuecomment-5615154736) 정책은 상호·지점·주소의
독립 근거가 일치해야 자동 채택하므로, 네이버 후보(2,055건)만으로는 업소를 확정하지 않는다. 사용자와 합의해
이 이슈에서는 0으로 기록하고, 사람 확인(`data/manual/gwangju/geocode.jsonl`)은 후속 작업으로 나눈다.
네이버 후보는 `geocode-lookup-v1.jsonl`에 남아 사람 확인에 쓸 수 있다.

### 미해결 원본 29개

폴백 정책에 따라 파일 단위 미해결로 남겼다. 잘 읽힌 일부를 레코드로 확정하지 않았다. 사용자 결정(2026-09-11):
상호 칸이 빈 지출(현금 경조사 등)과 원본 자체 합계 불일치는 정책대로 미해결로 둔다.

| 원본 | 게시 부서 | 사유 | 위치 |
| --- | --- | --- | --- |
| 10818-1·10819-1·10882-1·10883-1·10943-1.pdf | 교육문화전문위원 | `unsupported_format` | PDF |
| 10839-1.pdf | 관리과 | `unsupported_format` | PDF |
| 10830-3·10961-3 | 총무과 | `unsupported_format` | 같은 게시글의 xlsx 두 개를 묶은 ZIP |
| 11017-1.xlsx | 국제교류담당관 | 상호 빈칸 | sheet1:R14 |
| 10905-1.xls · 11000-1.xlsx | 문화정책관 | 상호 빈칸 | sheet1:R14 · R19 |
| 10939-1.xls | 총무부 | 상호 빈칸 | sheet1:R15 |
| 10938-1.xls | 대변인 | 상호 빈칸 | sheet1:R227 |
| 10922-1.xls · 10933-1.xls | 소방행정과 | 상호 빈칸 | sheet1:R5 · R6 |
| 10960-1.xlsx | 대중교통과 | 상호 빈칸 | sheet1:R60 |
| 10825-1.xls · 10929-1.xls | 체험지원과 | 상호 빈칸 | sheet1:R9 · R8 |
| 10954-1.xls | 국제교류담당관 | 합계 불일치 | sheet1:R21(합계 수식이 마지막 행을 빠뜨림) |
| 10786-1.xls | 소방행정과 | 합계 불일치 | sheet1:R3 |
| 11015-1 · 10991-1 · 10990-1 · 10909-1.xls | 관광도시과·아동청소년과·콘텐츠산업과·자원순환과 | 합계 불일치 | sheet1:R4 |
| 10956-1.xlsx | 경영부 | 합계 불일치 | sheet1:R4 |
| 10999-1 · 10977-1.xls | 연구지원과 | 합계 불일치 | sheet1:R5(10977은 합계가 첫 행을 빠뜨림) |
| 10817-1.xlsx · 10801-1.xls | 관리부·총무부 | 합계 불일치 | sheet3:R4 · R5 |

원본 파일명은 `<게시글 번호>-<원본 번호>`다. 합계 불일치 11개는 모두 평범한 `계`·`합계` 행이 같은 표의
항목 합과 다른 경우였고, 누계·기간 합계로 생긴 불일치는 없었다. 미해결 원본에 든 기간 안 행은
상호 빈칸 원본 약 450행, 합계 불일치 원본 약 180행으로 어림했다(자동 집계, 사람 전수 대조 전).
제출 전 미해결 0개 기준은 [폴백 정책](../specs/header-mapping-fallback.md)대로 남는다.

### 개인정보와 보존 필드

`records.csv`는 계약의 9개 필드만 가진다. 사용자·인원·결제방법·비목 열은 헤더 매핑에서 역할을 주지 않아
추출하지 않는다. 부서 열이 없으면 게시글의 작성 부서를 쓴다(66개 부서).

확인한 방법:

1. 원본에서 본 모양으로 규칙을 만들고 합성 이름으로 테스트했다(`tests/test_privacy.py`).
   연락처·주민등록번호·이메일, 경조사 목적의 괄호(수령인 소속·직급·이름), 가린 이름(`김ㅇㅇ`, `한*지`)을 지운다.
2. 경조사 지출의 상호 칸에 적힌 사람 이름 25건을 `개인(성명 비공개)`로 가렸다(사용자 결정). 가린 상호는
   비식당·판단 보류로 판별되어 네이버 조회에 보내지 않았다.
3. 원본에서 확인한 실명·가린 이름 25개로 `data/` 전체를 검색해 0건이었다.
4. 경조사 관련 목적 58종을 사람이 읽고 이름이 남지 않은 것을 확인했다. `이름 + 직함` 패턴 검색에서도
   실명은 없었다(`기자`·`국회의원`·`장흥군` 같은 일반어만).

경조사 문맥의 짧은 실제 상호(화환 가게 등)가 가려질 수 있다. 괄호 밖·경조사가 아닌 문맥의 실명은 규칙이
잡지 못하므로, 도시를 추가할 때마다 목적 목록을 사람이 다시 본다.

### 브라우저 확인

`http://127.0.0.1:8765/gwangju/`를 Chrome에서 두 번 열었다(수정 전 산출물 2,912건, 최종 2,921건).
창이 가려진 상태여서(`visibilityState: hidden`) 스크린샷은 찍지 못했고, 페이지 스크립트로 상태를 읽었다.

| 항목 | 결과 |
| --- | --- |
| 도시 화면 | 제목 `광주 공무원 맛집 지도`, 지도 타일 58장·네이버 저작권 표기·5km 축척, `.map-error` 안내 없음, 콘솔 오류 없음 |
| 마커 | `markers.json`만 먼저 받음. `0곳 전체 · 0곳 현재 지도 영역` |
| 첫 준비 지표 | `marker-data` 39–48ms. `first-ready`는 첫 확인에서 1,286ms, 최종 확인에서는 가려진 창이라 기록되지 않음 |
| 장부 | 탭을 연 뒤 `records.json`을 받음. `전체 2,921건 · 100건 표시`, `지오코딩 실패 · 주소 근거 없음`·`비식당`·`후보 없음` 사유 표시 |
| 자료 범위 | 대상 기간 2026년 상반기, `광주광역시청 · 수집 완료 · 레코드 2,921건` |

### LLM 비용

단가는 사용자가 요금 페이지에서 확인한 `gemini-3.6-flash` 1M 토큰당 입력 0.75 · 출력 3.75 USD다
(2026-12-31까지의 프로모션 단가). 기존 사용액은 사용자가 결제 콘솔에서 확인한 0달러로 장부에 적었다.
헤더 매핑 322회 · 비식당 판별 34회, 정산 합계 **USD 0.653**, 미정산 예약 0건이다. 헤더 매핑 호출의
대부분은 아래 캐시 결함과 규칙을 고치기 전의 재호출이다. 지금은 답변 이력(`headermap-answers-v1.jsonl`)과
캐시 변형 조회 덕분에 같은 입력의 재실행이 모델을 부르지 않는다(마지막 두 번의 재실행에서 0회).

### 확인하면서 찾은 것

- **캐시 변형과 첫 지출 누락(수정함, `b9e5581`)**: 같은 헤더가 원본마다 2·3·4행에 있거나 헤더 아래 요약 행
  유무가 달랐다. 서명 캐시의 최신 revision 하나만 보던 조회가 번갈아 실패해 한 키에 revision이 172개 쌓였고,
  "헤더 두 행 아래부터"로 배운 매핑이 요약 행 없는 표의 **첫 지출 9건을 검증 없이 건너뛰었다**.
  이제 같은 키의 변형을 모두 후보로 보고, 헤더와 데이터 시작 사이의 지출 행은 검증 실패로 둔다.
  수정 뒤 레코드는 9건 늘었고 바뀌거나 빠진 레코드는 없었다. `headermap.jsonl`의 286줄 중 대부분은
  이 결함이 남긴 이력이며, 추가형 캐시 정책에 따라 지우지 않았다.
- **지오코딩 이력 크기**: `geocode-history-v2.jsonl`은 레코드·분류가 바뀔 때마다 모든 판정을 새 키로
  다시 쌓는다(#37 설계). 광주시청만으로 한 번에 약 5MB라, 입력이 서너 번 더 바뀌면 파일당 20MB 상한에
  닿는다. 이번에는 커밋 전 중간 실행의 이력만 지웠다. 분할이나 보존 기준은 후속 결정이다.
- **Strict OOXML**: xlsx 2개가 ISO Strict 형식이었다. openpyxl은 이런 파일을 시트 0개로 조용히 읽는다.
  이름공간을 바꿔 읽게 하고, 시트가 없는 통합문서는 `unreadable`로 남긴다.
- **매크로 바이러스 잔재**: 총무과 xlsx에 1998년 Excel 매크로 바이러스(Classic.Poppy)가 남긴
  `XL4Poppy` 시트가 있다. 값만 읽기 모드로 읽으므로 실행되지 않고, 모델은 이 시트를 `layout=none`으로 판정했다.
- **누적 재게시**: 부서가 누적 파일(1월, 1~2월, 1~3월…)을 다시 올려 같은 지출이 여러 원본에 반복된다.
  원본을 넘어 날짜·상호·금액·부서가 같은 레코드가 118묶음(추가 124건) 있다. 자동으로 합치지 않았다.
  장부의 건수가 부풀 수 있으므로 병합 기준은 후속 결정이다.
- **네이버 키**: 처음 `.env`의 검색 키는 개발자센터 구형 키여서 API HUB가 401을 돌려줬다.
  사용자가 API HUB 키로 바꾼 뒤 조회했다. 잘못된 키로 만든 geocode 산출물은 커밋 전에 지웠다.
- **이용 조건**: 게시글에 공공누리 4유형(출처표시·상업적이용금지·변경금지)이 표시되어 있다.
  README의 데이터 라이선스 문단은 원 자료의 공공누리 조건이 우선한다고 적는다.

### 자동 검사

| 검사 | 결과 |
| --- | --- |
| `uv run ruff check .` | 통과 |
| `uv run ruff format --check .` | 통과 |
| `uv run mypy src` | 통과, 37개 소스 파일 |
| `uv run pytest` | 261 passed |
| `node --test tests/site_behavior.test.js` | 11 passed |
| `git diff --check` | 통과 |
| gitleaks(pre-commit 훅) | 커밋마다 no leaks found |

테스트는 네트워크를 막고 합성 게시판·합성 xls/xlsx·가짜 모델·가짜 네이버 응답만 쓴다.

### 남은 일

- 광주 5개 자치구 게시판 확인·선언과 수집(#13의 광주 완료 기준)
- PDF·ZIP 묶음 원본 처리, 상호 빈칸·합계 불일치 원본의 사람 전수 대조와 제출 전 미해결 0개
- 주소가 없는 레코드의 사람 업소 확인으로 마커 만들기
- 누적 재게시로 반복된 레코드의 병합 기준, 지오코딩 이력의 크기 관리
- 실데이터 성능 측정: [#29](https://github.com/snowjaewon/OfficialDeliciousMap/issues/29)(이 기록의 수치가 입력)
